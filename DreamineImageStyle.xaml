<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Dreamine.UI.Wpf.Controls">
    <Style TargetType="{x:Type local:DreamineImage}">
        <!--  기본값 설정  -->
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BorderBrush" Value="#FF88AACC" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Stretch" Value="Uniform" />

        <!--  컨트롤의 시각적 구조를 정의하는 템플릿  -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DreamineImage}">
                    <!--  Border가 테두리와 둥근 모서리를 담당합니다.  -->
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">

                        <!--  Border 내부에 Image를 배치하여 이미지를 표시합니다.  -->
                        <!--  Border의 CornerRadius가 Image를 자동으로 클리핑합니다.  -->
                        <Image
                            x:Name="PART_Image"
                            RenderOptions.BitmapScalingMode="HighQuality"
                            Source="{TemplateBinding Source}"
                            Stretch="{TemplateBinding Stretch}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  올바르게 수정된 트리거  -->
                        <DataTrigger Binding="{Binding Source, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                            <Setter TargetName="PART_Image" Property="Source" Value="{Binding FallbackSource, RelativeSource={RelativeSource Self}}" />
                        </DataTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>